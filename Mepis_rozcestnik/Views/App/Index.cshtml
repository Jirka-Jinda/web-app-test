@model Mepis_rozcestnik.Models.announcements_collection

@foreach (announcements an in Model.collection)
{
    var class_name = "";
    @if (@an.is_warning) 
        class_name = "waring-message";

    <article class=@class_name>
        <time datetime=@an.Date>??</time>
        <h3>@an.header</h3>
        <div class="article-content">
            <p>
                @an.content
            </p>
        </div>
    </article>
}